#ifndef checker_h
#define checker_h

#include <iostream>
#include <string>
#include <vector>
#include <list>
#include <stdio.h>
#include <fstream>
#include "parser.h"

using namespace std;

void check(vector <float> contacts, vector < vector < vector < int> > > userToSat, defined_data its_data);

#endif
